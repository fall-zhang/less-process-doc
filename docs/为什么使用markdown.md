
## 什么是 Markdown

markdown 是轻量标记文本，使用简单的标记即可实现对文档的编辑

## 如何使用 markdown

新建一个 markdown 文件，在里面输入


注释区域

因为可以做备忘录
因为可以永久保存
因为占用空间小因为存储介质便宜
因为可以多种转译


任何应用访问剪切板时，可能有敏感数据（账号密码等），请在每次复制粘贴后清除这些内容。

windows：win 键 + v

- 百度脑图迁移

## 为什么使用 markdown

### 便于查找

因为笔记便于查找
如果是语音，视频记录的信息，那怕十倍速播放，也要查好久
并且对于和机器的交互，你无法凭空输入一段视频，或者是图片，只能是文字描述
对于查找效率，文字最高，音频次之（转文字后，可以根据文字搜索），图片次之，视频更次

### 空间据用小

一首无损音乐，10M，一张图片，100k，一段视频 10M

1MB，大致等于一本书，256,000字
1G，等于一千本书，
5G，等于五千本书
如果线下模式，用户想用多少用多少

为什么要做笔记？

### 自定义样式

可以一键更改生成的样式

代码高亮